{
  "name": "response-to-data-url",
  "description": "Turn a fetch() response into a `data:` URL.",
  "author": "Gerben <gerben@treora.com>",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Treora/response-to-data-url.git"
  },
  "license": "Unlicense",
  "files": ["lib"],
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "watch": "babel src -d lib -w",
    "prepublish": "npm run build",
    "postinstall": "postinstall-build lib"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-1": "^6.24.1"
  },
  "dependencies": {
    "postinstall-build": "^3.0.1"
  }
}
